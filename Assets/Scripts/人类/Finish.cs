using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Finish : MonoBehaviour
{
    Humon humon;
    // Start is called before the first frame update
    void Start()
    {
        humon = GetComponent<Humon>();
    }

    // Update is called once per frame
    void Update()
    {
    }
}
